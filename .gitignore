# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr
build
*.tsbuildinfo
.tmp

# Test & coverage artefacts
coverage
.vitest
*.lcov

# Environment / secrets — never commit anything but the templates
.env
.env.*
!.env.example
!*.env.example
.dev-zone-keys.json
.env.simulator

# Database dumps & backups
backups/
*.dump
*.sql.gz

# PlatformIO build output and downloaded library sources (firmware/)
.pio/
.piolibdeps/

# Real zone credentials for the ESP32 node — generated, never committed
firmware/include/zone_secrets.h

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local
firmware/include/zones/
firmware/logs/
firmware/include/zone_registry.h

# Demo prep notes — kept local, not part of the submission
docs/demo-script.md
