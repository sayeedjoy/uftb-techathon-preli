# Root-level environment used by docker-compose.yml.
# Per-package configuration lives in backend/.env.example and frontend/.env.example.

POSTGRES_USER=scsrg
POSTGRES_PASSWORD=scsrg
POSTGRES_DB=scsrg
POSTGRES_HOST_PORT=5433
